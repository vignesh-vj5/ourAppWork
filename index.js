/**
 * @format
 */

import {AppRegistry} from 'react-native';
//import App from './App';
import {name as appName} from './app.json';
import AppR from './src/AppR';

AppRegistry.registerComponent(appName, () => AppR);
